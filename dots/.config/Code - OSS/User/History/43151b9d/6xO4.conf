-- Conky, a system monitor https://github.com/brndnmtthws/conky
--
-- This configuration file is Lua code. You can write code in here, and it will
-- execute when Conky loads. You can use it to generate your own advanced
-- configurations.
--
-- Try this (remove the `--`):
--
--   print("Loading Conky config")
--
-- For more on Lua, see:
-- https://www.lua.org/pil/contents.html
--
-- Conky Lua API: https://conky.cc/lua

-- Configuration settings: https://conky.cc/config_settings
conky.config = {
    alignment = 'top_left',
    background = false,
    border_width = 1,
    cpu_avg_samples = 2,
    default_color = '#b9bdb6',
    default_outline_color = 'white',
    default_shade_color = '#5c17ca',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = false,
    draw_outline = false,
    draw_shades = true,
    extra_newline = false,
    font = 'Hack:size=14',
    gap_x = 40,
    gap_y = 30,
    minimum_height = 5,
    minimum_width = 550,
    maximum_width = 550,
    net_avg_samples = 2,
    no_buffers = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_wayland = true,
    out_to_x = false,
    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'normal',
    own_window_hints = 'undecorated,sticky,below,skip_taskbar,skip_pager',
    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    update_interval = 3.0,
    uppercase = false,
    lowercase = true,
    use_spacer = 'none',
    use_xft = true,
    short_units = true,
}

-- Variables: https://conky.cc/variables

conky.text = [[
#b9bdb6  Green
#6da3bb	 Blue
#cf8a15  Yellow
#5c17ca  Red
$alignc${color #256a79}${font Hack:Regular:size=75}${time %H:%M}${font Hack:size=5}


$alignc${color #256a79}${battery_bar 13,250 BAT1}
$alignc${color #db7909}${font Hack:Bold:size=14}${battery_percent BAT1}% ${battery_status BAT1}

$alignc${color grey}$color$nodename $kernel
${color #256a79}$hr
$alignc${color light blue}${wireless_essid wlp166s0}
$alignc${color grey}up: ${upspeed wlp166s0} ${color grey} - down:$color ${downspeed wlp166s0}

${color grey}ram$alignr$color $mem/$memmax
${color #db7909}  ${membar 15, 250} $color$memperc%
${color grey}cpu 
${color #db7909}  ${cpubar 15, 200} $color$cpu%
${color grey}fs$alignr$color  ${fs_used /} / ${fs_size /}
${color #db7909}  ${fs_bar 15, 150 /} $fs_used_perc%
${color grey}swap $alignr $color $swap/$swapmax
${color #db7909}  ${swapbar 15, 100} $color$swapperc%



































${color #256a79}$hr
${color grey}Name                   PID       CPU%     MEM%
${color lightgrey}${top name 1}     ${top pid 1}   ${top cpu 1}   ${top mem 1}
${color lightgrey}${top name 2}     ${top pid 2}   ${top cpu 2}   ${top mem 2}
${color lightgrey}${top name 3}     ${top pid 3}   ${top cpu 3}   ${top mem 3}
${color lightgrey}${top name 4}     ${top pid 4}   ${top cpu 4}   ${top mem 4}
]]
